{
  "name": "scae-backend",
  "version": "1.0.0",
  "description": "API REST para el Sistema de Control de Asistencias (SCAE)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "init-db": "mysql -u $DB_USER -p$DB_PASSWORD -h $DB_HOST $DB_NAME < schema.sql"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.0"
  }
}
